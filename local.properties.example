# 示例 local.properties — 复制为 `local.properties` 后填入本机路径。
# 此文件 **不入库**，提交前请检查 .gitignore。
#
# Android Studio 首次打开项目时会自动生成 local.properties，可直接编辑。

# ── 必填：Android SDK 路径 ───────────────────────────────────────────────
# macOS:   sdk.dir=/Users/<you>/Library/Android/sdk
# Linux:   sdk.dir=/home/<you>/Android/Sdk
# Windows: sdk.dir=C\:\\Users\\<you>\\AppData\\Local\\Android\\Sdk
sdk.dir=/path/to/Android/sdk


# ── 可选：本地 Release 签名（见 docs/RELEASE.md） ───────────────────────
# 推荐改放在 ~/.gradle/gradle.properties，避免误提交。
# 如果一定要放这里，确保 local.properties 在 .gitignore 中。
#
# HAUI_KEYSTORE_FILE=/path/to/haui-release.keystore
# HAUI_KEYSTORE_PASSWORD=
# HAUI_KEY_ALIAS=haui
# HAUI_KEY_PASSWORD=
