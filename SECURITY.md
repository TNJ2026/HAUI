# 安全策略

## 报告漏洞

发现安全相关问题，请**不要走公开 Issue**，避免在补丁发布前被利用。

- 推荐渠道：发送邮件至 `chengxd3@gmail.com`，主题前缀 `[HAUI Security]`。
- 请尽量附带：复现步骤、受影响版本/commit、潜在影响、修复建议（如有）。
- 我们会在 **3 个工作日** 内回执，并就修复时间与你同步进展。

## 适用范围

下列内容属于本项目的安全范畴，欢迎报告：

- 客户端凭据/会话泄漏（`SecurePrefs`、DataStore、日志）。
- 与 Hermes 服务端通信路径上的协议漏洞。
- ProGuard/R8 配置导致的代码暴露或反射安全问题。
- 第三方依赖在本项目中的实际利用路径。

下列**不在**本项目范围内，请向上游报告：

- Hermes 服务端自身的漏洞。
- 第三方库的通用 CVE，除非本项目的使用方式构成新的利用面。
- 不影响 Release 构建的 Debug 模式行为（例如 `BuildConfig.DEBUG` 下额外日志）。

## 披露原则

修复发布后，我们会在 `CHANGELOG.md` 安全条目下致谢报告者（如果同意公开），并标注影响版本范围。
